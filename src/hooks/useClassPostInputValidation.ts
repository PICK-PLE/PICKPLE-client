// import { components } from "@schema";

// type Category = components['schemas']['CategoryInfo']
// type DateInfo = components['schemas']['DateInfo']
// type AccountInfo = components['schemas']['AccountInfo']

// export const useClassPostInputValidation = () => {
//   const validateLength = (answer: string) => {
//     return answer.trim().length >= 1;
//   };

//   const validateCategory = (category: Category) => {
//     return !!category.category1 || !!category.category2 || !!category.category3;
//   };

// const validateStepOne = (state: {
//     categoryList: Category;
//     spot: string;
//     dateList: DateInfo;
//     maxGuest: number;
//     fee: number;
//     accountList: AccountInfo;
// }) => {
//     const isCategoryValid = validateCategory(state.categoryList);
//     const isSpotValid = validateLength(state.spot);
//     const isDateListValid = 
// }
//   return {
//     validateLength,
//   };
// };
