import { IconButton } from '@components';
import { IcCopyPlus } from '@svg';

const Dev = () => {
  return (
    <div>
      <IconButton icon={<IcCopyPlus />} onClick={() => {}}>
        작성하기
      </IconButton>
    </div>
  );
};

export default Dev;
